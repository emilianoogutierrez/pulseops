# Security

Do not open public issues containing credentials, session tokens or customer data.

The repository ships synthetic demo data only. Production deployments should use separate secrets, HTTPS, secure cookies, database backups and provider credentials managed outside browser-accessible state.

For a private disclosure, contact the maintainer through the email listed on the GitHub profile.

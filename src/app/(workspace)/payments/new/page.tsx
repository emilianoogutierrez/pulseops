import Link from "next/link";
import { ArrowLeft, CreditCard, Link2, ShieldCheck } from "lucide-react";
import { PageHeader } from "@/components/ui/page-header";
import { requireSessionIdentity } from "@/server/auth/session";
import { prisma } from "@/server/db/prisma";
import { createPaymentAction } from "../create-action";
export default async function NewPayment() { const s = await requireSessionIdentity(); const [clients, projects] = await Promise.all([prisma.client.findMany({ where: { workspaceId: s.workspaceId }, orderBy: { companyName: "asc" } }), prisma.project.findMany({ where: { workspaceId: s.workspaceId }, include: { client: true }, orderBy: { updatedAt: "desc" } })]); return <><PageHeader eyebrow="Collections" title="New payment request" description="Register the external payment path and reconcile settlement later." action={<Link className="button button--secondary" href="/payments"><ArrowLeft size={15}/>Payments</Link>}/><form action={createPaymentAction} className="editor-layout"><section className="editor-main surface"><div className="section-title"><div><span className="eyebrow">Request</span><h2>Settlement details</h2></div></div><div className="form-grid"><label className="field"><span>Client</span><select name="clientId" required><option value="">Select client</option>{clients.map(c => <option key={c.id} value={c.id}>{c.companyName}</option>)}</select></label><label className="field"><span>Project (optional)</span><select name="projectId"><option value="">No linked project</option>{projects.map(p => <option key={p.id} value={p.id}>{p.client.companyName} · {p.name}</option>)}</select></label><label className="field"><span>Method</span><select name="method"><option value="SPEI">SPEI</option><option value="MERCADO_PAGO">Mercado Pago</option><option value="USDT_BINANCE">USDT · Binance</option><option value="OTHER">Other</option></select></label><label className="field"><span>Amount</span><input type="number" name="amount" min="1" required/></label><label className="field"><span>Currency</span><input name="currency" defaultValue="MXN" maxLength={3}/></label><label className="field"><span>Due date</span><input type="date" name="dueAt"/></label><label className="field field--wide"><span>External reference</span><input name="externalReference" placeholder="SPEI reference, Binance reference, or merchant id"/></label><label className="field field--wide"><span>Payment URL</span><input type="url" name="paymentUrl" placeholder="https://link.mercadopago.com.mx/…"/></label></div></section><aside className="editor-side surface"><div className="editor-side__head"><CreditCard size={18}/><div><strong>External settlement</strong><span>PulseOps records; it does not custody funds.</span></div></div><div className="quality-note"><ShieldCheck size={17}/><p>SPEI is Mexico-first. Mercado Pago stores an operator-generated payment link. USDT/Binance is the international fallback.</p></div><div className="quality-note"><Link2 size={17}/><p>Payment URLs are references only; settlement is explicitly marked after verification.</p></div><button className="button button--primary button--full">Create request</button></aside></form></>; }

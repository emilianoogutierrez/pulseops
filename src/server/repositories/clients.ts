import { prisma } from "@/server/db/prisma";
export async function listClients(workspaceId: string) { const clients = await prisma.client.findMany({ where: { workspaceId }, include: { opportunities: true, projects: true, payments: true }, orderBy: { updatedAt: "desc" } }); return clients.map(c => ({ id: c.id, name: c.companyName, contact: c.contactName, method: c.preferredPaymentMethod ?? "UNCONFIRMED", source: c.acquisitionSource ?? "Unknown", lifetime: c.payments.filter(p => p.status === "PAID").reduce((s, p) => s + Number(p.amount), 0), projects: c.projects.length, opportunities: c.opportunities.length })); }
export async function getClient(workspaceId: string, id: string) { return prisma.client.findFirst({ where: { id, workspaceId }, include: { opportunities: { orderBy: { updatedAt: "desc" } }, projects: { orderBy: { updatedAt: "desc" } }, payments: { orderBy: { createdAt: "desc" } } } }); }
